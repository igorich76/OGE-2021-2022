Напишите программу, которая в последовательности натуральных чисел определяет количество чисел, 
являющихся делителями числа 2020, но не совпадающих с ним самим. Программа получает на вход количество чисел в последовательности, 
а затем сами числа. В последовательности всегда имеется число, являющееся делителем числа 2020, но не совпадающее с  ним самим.
Кол-во чисел не превышает 1 000. Введенные числа не превышают 30 000. 
Программа должна вывести одно число - количество чисел, являющихся делителем числа 2020, но не совпадающее с ним самим.

##
var n := ReadlnInteger('Введите количество чисел >>');
var a: integer;
var k := 0;
for var i := 1 to n do
begin
  readln(a);
  if (2020 mod a = 0) and (a <> 2020)
    then k += 1;
end;
Println('Количество чисел >>', k);
//// а можно так:
##
var n := ReadlnInteger('Введите количество чисел >>');
var k := 0;
for var i := 1 to n do
begin
  var a := ReadlnInteger; // инициализация переменной в цикле
  if (2020 mod a = 0) and (a <> 2020)
    then k += 1;
end;
Println('Количество чисел >>', k);
