Напишите программу, которая в последовательности натуральных чисел определяет количество чисел, 
являющихся делителями числа 4040, но отличных от единицы. Программа получает на вход количество чисел в последовательности, 
а затем сами числа. В последовательности всегда имеется число, являющееся делителем числа 4040, но отличные от единицы.
Кол-во чисел не превышает 1 000. Введенные числа не превышают 30 000. 
Программа должна вывести одно число - количество чисел, являющихся делителем числа 4040, но отличных от единицы.
##
var N := ReadInteger('Введите длину  последовательности:');
var cnt := 0;
for var i:=1 to N do
  begin
    var a := ReadInteger('Число:');
    if (4040 mod a = 0) and (a <> 1) then 
      begin
        Println('+');
        cnt += 1;
      end
      else Println('no');
  end;
Print('Делителей:',cnt);
// чет херня какая-то, задача правильная, а в тестах ошибки или что, в тестах другие данные...
