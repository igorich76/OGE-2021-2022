Напишите программу, которая в последовательности натуральныых чисел определяет количество чисел, больших 100.
Программа получает на вход количество чисел в последовательности, а затем сами числа.
В последовательности всегда имеется число, большее 100.
Количество чисел не превышает 1000. Введённые числа не превышают 30 000.
Программа должна вывести одно число: количество чисел, больших 100.

##
var n := ReadlnInteger('Введите длину последовательности:');
var cnt := 0;
for var i := 1 to n do
  begin
    Print('Введите', i,'-е число:');
    var a := ReadlnInteger;
    if (a > 100) then cnt += 1;
  end;
Print(cnt);
